import Loader from '../Loader';
import { PageLoaderContainer } from './page-loader.styled';

const PageLoader = () => {
  return (
    <PageLoaderContainer>
      <Loader />
    </PageLoaderContainer>
  );
};

export default PageLoader;
